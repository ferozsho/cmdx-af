# AgentForge Application Environment Configuration
APP_ENV=development
APP_MODE=production  # Options: production

# Cloud API Settings
API_HOST=0.0.0.0
API_PORT=8000
SECRET_KEY=sk-proj-8f9d2c4b1e7a6f3d9c2e5b8a1f4d7c9e2b5a8f3d7c9e2b5a8f3d7c9e2b5a8f

# Database & Cache
DATABASE_URL=postgresql+asyncpg://agentforge:agentforge123@localhost:5432/agentforge_db
REDIS_URL=redis://localhost:6379/0

# Qdrant Vector DB
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=

# LLM Providers (DeepSeek) — configured via the Settings page
# (saved to apps/api/data/runtime_settings.json), not environment variables.

# RAG Configuration — managed via the Settings page
# (saved to apps/api/data/runtime_settings.json), not environment variables.

# Security & Safety
ENABLE_CONFIRMATION_BEFORE_DELETE=true
GIT_AUTHOR_NAME=AgentForge AI
GIT_AUTHOR_EMAIL=agent@agentforge.ai
